{
  "name": "psstore",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "lint": "npm run lint -w shared && npm run lint -w server && npm run lint -w client",
    "typecheck": "npm run build -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "test": "npm run test -w shared && npm run test -w server && npm run test -w client",
    "build": "npm run clean && npm run build -w shared && npm run build -w client && npm run build -w server",
    "clean": "rm -rf build client/build server/dist shared/dist",
    "start": "npm run start -w server"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@types/express": "^5.0.1",
    "@types/luxon": "^3.6.2",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@typescript-eslint/eslint-plugin": "^8.24.1",
    "@typescript-eslint/parser": "^8.24.1",
    "@vitejs/plugin-react-swc": "^3.8.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.20.1",
    "eslint-plugin-react": "^7.37.4",
    "prettier": "^3.5.3",
    "stylelint": "^16.14.1",
    "stylelint-config-standard": "^37.0.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
